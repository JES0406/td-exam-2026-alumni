apiVersion: v1
kind: Service
metadata:
  name: svc-api
spec:
  type: LoadBalancer # Exponemos al exterior usando LoadBalancer
  selector:
    app: api-gw
  ports:
    - port: 80
      targetPort: 3000
